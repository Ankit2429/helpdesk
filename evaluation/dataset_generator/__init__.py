"""Automatic Evaluation Dataset Generator Package.

This package parses Markdown documents from the knowledge base, classifies them by domain,
synthesizes grounded question-answer pairs, validates & deduplicates queries, and outputs
structured benchmark datasets to evaluation/datasets/*.json.
"""

__version__ = "0.1.0"
